# The One 影響力會員 Demo｜技術開發手冊

文件日期：2026-08-29  
對應規格：三層架構 v2.1（知遇卡／知音卡／知己卡）＋五項年度計畫與募集額滿  
正式站：https://the-one-influence-membership.vercel.app  
原始碼：https://github.com/goodylin123-design/the-one-influence-membership

---

## 1. 這份 Demo 是什麼

這是給內部利害關係人操作的單頁示範，用來走一遍「影響力消費 → 選卡 → 鎖定年度支持計畫 → 未來提貨券 → 每年核對」的機制。**不是正式上線產品。**

刻意不做的事：

- 真實登入、註冊、權限
- 金流、發票、退款
- 後端 API、資料庫、多會員帳號
- 後台管理、Email／推播
- 真實邀請碼核銷或庫存控管

所有內容是 mock data，狀態只活在瀏覽器記憶體。重新整理或按「重新體驗」即重置。

---

## 2. 技術棧

| 項目 | 選擇 | 說明 |
| --- | --- | --- |
| 執行環境 | Node.js（本機開發用） | 靜態前端，無伺服器邏輯 |
| 建置 | Vite 6 | `base: "./"`，可放子路徑 |
| UI | React 19 + TypeScript | 嚴格型別，畫面用前端 state 切換 |
| 樣式 | Tailwind CSS v4 | 經 `@tailwindcss/vite` 接入；色票寫在 `src/index.css` 的 `@theme` |
| 狀態 | React Context | `src/context/AppContext.tsx`，不使用 Redux／路由庫 |
| 字體 | Google Fonts | 標題 Noto Serif TC、內文 Noto Sans TC |
| 部署 | Vercel 靜態站 | 帳號 `goodylin123-design`，production alias 如下 |

本機：

```bash
npm install
npm run dev          # http://localhost:5173/
npm run build        # tsc -b && vite build
npm run preview
```

發佈正式站（需已登入 Vercel CLI，且工作目錄為專案根）：

```bash
vercel --prod --yes
```

推送到 GitHub 的 `main` 後，Vercel 會自動部署正式站。CLI `vercel --prod --yes` 可用，但以 GitHub `main` 為準，避免兩套部署互相覆蓋。

---

## 3. 目錄與職責

```
src/
  main.tsx                 掛上 AppProvider
  App.tsx                  依 screen 渲染單一畫面
  index.css                品牌色、字體、panel／會員卡／票券樣式
  types.ts                 領域型別（唯一來源）
  context/AppContext.tsx   全域狀態與流程動作
  data/mock.ts             卡別、計畫、附加禮遇、示範會員
  lib/format.ts            年費、萬元募集金額、日期、券號、依模擬時間推導券狀態
  screens/                 七個畫面
  components/              共用 UI（頂欄、鎖定彈窗、提貨券、附加禮遇表、圖示）
```

新增畫面時：在 `types.ts` 的 `Screen` 加字串 → `App.tsx` 分支 → Context 提供導向動作。不要引入 React Router，除非產品已確定要可分享的深層網址。

---

## 4. 產品規則（實作必須遵守）

### 4.1 核心與加碼

- **核心**：年度支持計畫 + 對應的未來提貨券。券是憑證（有編號、鎖定日、確定兌現日），不是點數。
- **附加禮遇**：依卡別逐級累加，不得搶走核心的敘事位置。知音含知遇全部，知己再往上加。

### 4.2 三張卡

| 卡別 | id | 年費 | 計畫上限 | 可選規格 | 申請 |
| --- | --- | --- | --- | --- | --- |
| 知遇卡 | `zhiyu` | NT$30,000 | 1 | 標準 | 開放 |
| 知音卡 | `zhiyin` | NT$100,000 | 2 | 標準＋進階 | 開放 |
| 知己卡 | `zhiji` | NT$500,000 | 不限 | 標準＋進階＋最高 | 邀請制 |

卡面顏色：知遇藍灰 `#4A6B7A`、知音墨綠 `#24422F`、知己酒紅 **`#7B0E0E`**（以使用者提供色票為準，不要改成泛用磚紅）。

累加判定用 `TIER_RANK`（1／2／3）與 `addOnsForTier()`，不要在畫面裡硬寫「這張卡有哪幾項」。

### 4.3 年度支持計畫

目前五項。募集數字為 Demo mock；`concert` 刻意設為額滿，用來示範「停止募集」。

| id | 名稱 | 規格 | 年限 | 目標 | Demo 已募 |
| --- | --- | --- | --- | --- | --- |
| `tofu` | 封藏豆腐乳 | 標準 | 2 年 | NT$20萬 | 12萬（60%） |
| `concert` | 專屬音樂會 | 進階 | 1 年 | NT$100萬 | 100萬（**額滿**） |
| `plumwine` | 工藝重現與熟成梅酒 | 最高 | 5 年 | NT$50萬 | 18萬 |
| `lunch` | 小朋友的營養午餐美學再造 | 標準 | 5 年 | NT$2000萬 | 420萬 |
| `hall` | 大地音樂廳計畫 | 進階 | 2 年 | NT$500萬 | 475萬（95%） |

`lunch`、`hall` 的規格文件未寫標準／進階／最高。Demo 將營養午餐設為**標準**（知遇卡起可支持為孩子的計畫），大地音樂廳設為**進階**（對齊音樂會）。若決策改成知己專屬，只改 `qualityTier`。

選計畫時以 `projectAllowed(...)` 過濾；達卡別上限或 `isFunded(project)`（已募 ≥ 目標）則不能再加入。知己卡 `maxProjects === null` 表示不限項數。

圖示：`projectIcon(category, id)`；`lunch`／`hall` 有專用圖示，其餘依飲食／音樂／工藝。

### 4.4 募集與額滿

每項計畫有 `targetAmount`、`raisedAmount`（新台幣整數）。畫面用 `formatWan()` 顯示「NT$xx萬」。

- 未額滿：顯示目標、已募、百分比進度條
- 額滿：文案「額滿，停止募集」，卡片不可點選（`AppContext.toggleProject` 也會拒絕）

Demo **不會**在鎖定後把已募金額加上去。正式產品必須把「年費」與「計畫募集進度」分開記帳，並由伺服器判定額滿。

### 4.5 邀請制

知己卡按「下一步」會進 `invite`。Demo 規則：**任一非空白字串即通過**。正式產品必須改成伺服器核銷，不可沿用此捷徑。

---

## 5. 畫面流

```
landing
  → selectTier
      → invite（僅知己卡）
      → selectProjects
          → LockModal（勾選同意後 confirmLock）
          → voucherReveal
          → dashboard
              → verify（時間快轉或點「核對」）
```

`TopBar` 的 The One 標誌回到 `landing`；「重新體驗」呼叫 `resetDemo()` 回到初始狀態。

畫面沒有 URL，無法用網址還原進度。展示時請從頭走一遍，或改完程式後整頁重新整理。

---

## 6. 狀態模型

`AppContext` 持有：

| 欄位 | 用途 |
| --- | --- |
| `screen` | 目前畫面 |
| `selectedTierId` | 已選卡 |
| `invitationVerified` | 知己邀請是否通過 |
| `selectedProjectIds` | 尚未鎖定的勾選 |
| `vouchers` | 鎖定後產生的未來提貨券 |
| `simulatedNow` | Demo 專用「現在」，所有進度以此計算 |
| `lockModalOpen` | 鎖定確認層 |
| `verifySnapshot` | 核對頁前後對照 |

鎖定（`confirmLock`）時：依每個已選計畫建立 `FutureVoucher`，兌現日 = 鎖定日 + `maturityYears`，編號由 `makeVoucherNumber()` 產生（格式 `TO-FV-{年}-{四位流水}`，Demo 用，非正式發行規則）。

進度（`deriveVoucherState`）：用鎖定日到 `simulatedNow` 的整年數對應 `project.stages` 的 index；達兌現年則 `status = redeemable`。

「模擬時間前進一年」會把 `simulatedNow` +1 年、重算所有券，並帶一筆 `VerifySnapshot` 進核對頁。優先展示梅酒或豆腐乳的階段變化，方便講「核對」這一步。

---

## 7. 改內容時改哪裡

| 要改什麼 | 檔案 |
| --- | --- |
| 年費、卡別文案、計畫上限 | `src/data/mock.ts` 的 `TIERS` |
| 附加禮遇項目與從哪一卡開始 | `ADD_ON_BENEFITS`（`minTierId`） |
| 計畫故事、階段文案、年限、募集目標與已募 | `PROJECTS`（`targetAmount`、`raisedAmount`） |
| 額滿判定 | `isFunded()`、`fundPercent()`；鎖定攔截在 `AppContext.tsx` |
| 計畫圖示 | `Icons.tsx` 的 `projectIcon(category, id)` |
| 對照表／儀表板呈現 | `AddOnTable.tsx`、`Dashboard.tsx` |
| 禮遇圖示 | `Icons.tsx` 的 `benefitIcon(id)`，id 必須對上禮遇 id |
| 色票與字級 | `src/index.css` |
| 流程規則（邀請、上限、鎖定） | `AppContext.tsx`，不要只改畫面 |

改 `types.ts` 時同步改 mock 與使用處，避免畫面出現 `undefined`。

---

## 8. 視覺約定

對齊 The One CRM Demo（https://the-one-crm-demo.vercel.app/）的溫度，不是 SaaS 藍紫風。

- 背景奶油 `#FBF5E7`，標題墨綠，強調焦橘 `#C25B2E`，分隔金 `#D9A441`
- 白底圓角卡片（`.panel`），動效偏慢（`.page-in`）
- 提貨券做成票根／印章感（`.ticket`），不要做成一般 app 列表列
- 附加禮遇對照：色點勾選＝享有，金圈＝未含；欄位色對應三張卡，不要用「有／無」文字矩陣

---

## 9. 展示腳本（給操作者）

1. Landing：預見／回應現在 vs 回應未來。
2. 選卡：說明三張卡差異（項數、規格、知己邀請制），捲到附加禮遇對照。
3. 建議走知音卡：可選標準＋進階；對照表與儀表板會看到第二層禮遇。
4. 選計畫：指出「專屬音樂會」已額滿；知音可改選「封藏豆腐乳」與「大地音樂廳計畫」。
5. 鎖定彈窗：勾選、強調恕不退費。
6. 提貨券畫面：指出編號與兌現日，「這不是點數」。
7. 儀表板按「模擬時間前進一年」，看核對頁前後文案。
8. 需要重來：按「重新體驗」。

知遇卡：可在豆腐乳與營養午餐之間擇一（上限 1 項）。  
知己卡：選知己 → 邀請畫面任意輸入 → 可選梅酒（最高規格）。

---

## 10. 開發注意

- **不要把 Demo 捷徑帶進正式產品**：空白即過的邀請碼、本機時間快轉、單一「示範會員」、鎖定後募集金額不增加。
- **年費 ≠ 計畫募集**：卡片年費與計畫 `targetAmount` 是兩套數字。Demo 只展示後者的進度條。
- **不要做金流樣板**：鎖定確認已足夠表達承諾；假刷卡頁會讓利害關係人誤以為已上線收款。
- **狀態不可持久化**：刻意不寫 `localStorage`，避免展示現場殘留上次操作。
- **提交範圍**：不要把 Word 暫存檔（`~$…docx`）、重複的 `.gitignore`、`.env*` 推進 Git。
- Git 使用者資訊若本機為空，提交時需指定 name／email（目前遠端帳號為 `goodylin123-design`）。

---

## 11. 與舊規格的差異

倉庫內 `Cursor專案說明_The_One影響力會員Demo.md` 仍是早期兩卡稿（知音 3 萬／知己 10 萬）。**目前程式以三卡 v2.1、五項計畫與募集額滿為準。** 改功能請對照本手冊與 `src/data/mock.ts`。
